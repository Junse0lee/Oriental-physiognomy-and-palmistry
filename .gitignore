# --- 기존 내용 보강 ---
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
.env

# --- 보안 (가장 중요) ---
# GCP 서비스 계정 키 파일이 실수로 올라가는 것을 방지
*.json

# --- 운영체제 및 에디터 ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- 프로젝트 특화 ---
# 로그 및 임시 파일
*.log
tmp/

# AI 모델 및 대용량 파일 (이미 모델이 있다면 포함하되, 
# 만약 Git LFS를 안 쓴다면 용량 초과 방지를 위해 체크 필요)
# .pt 파일은 배포 시 필요하므로 제외하지 않지만, 
# 학습 중 생기는 임시 체크포인트는 제외하는 게 좋습니다.
runs/